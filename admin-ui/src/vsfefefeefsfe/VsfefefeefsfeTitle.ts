import { Vsfefefeefsfe as TVsfefefeefsfe } from "../api/vsfefefeefsfe/Vsfefefeefsfe";

export const VSFEFEFEEFSFE_TITLE_FIELD = "id";

export const VsfefefeefsfeTitle = (record: TVsfefefeefsfe): string => {
  return record.id || String(record.id);
};
